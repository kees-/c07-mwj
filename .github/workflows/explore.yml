name: Ping files

on: workflow_dispatch

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3

      - name: Test activities
        run: |
          pwd
          ls /
          tree -dL 2 /
